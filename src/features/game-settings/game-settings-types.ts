export enum GameModes {
    Start = "start",
    Pause = "pause",
    Reset = "reset",
}

export enum GameFieldSize {
    Small = "sm",
    Medium = "md",
    Large = "lg",
}

export enum GameSpeeds {
    OneX = "1x",
    FiveX = "5x",
    TenX = "10x",
    TwentyX = "20x",
}
