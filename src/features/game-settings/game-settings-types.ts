export enum GameModes {
    Start = "start",
    Pause = "pause",
    Reset = "reset",
}

export enum GameFieldSize {
    Small = "sm",
    Medium = "md",
    Large = "lg",
}

export enum GameSpeeds {
    OneX = 1,
    FiveX = 5,
    TenX = 10,
}
